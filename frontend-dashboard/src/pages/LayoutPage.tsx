import { Map } from "lucide-react"

import { PlaceholderPage } from "@/components/layout/PlaceholderPage"

export function LayoutPage() {
  return (
    <PlaceholderPage
      description="Map S1 ambient and S2 hotspot placement on the server testbed layout."
      icon={Map}
      sectionDescription="Image upload and draggable ratio-based sensor markers arrive in the layout milestone."
      sectionTitle="Testbed layout workspace"
      title="Layout"
    />
  )
}
