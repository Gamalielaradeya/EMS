# Raspberry Pi Gateway

Python gateway foundation for Raspberry Pi 3 and two XY-MD02 sensors over USB RS485.

The gateway remains hardware-first. It will read and send raw sensor data every 10 seconds and send heartbeat status every 60 seconds.

Implementation begins in Milestone `3`.
